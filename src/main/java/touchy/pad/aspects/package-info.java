/**
 * Aspects are used to seperate out cross-cutting concerns.
 *
 * Spring will apply these aspects, so we can only advice public methods of
 * beans (meaning by injected objects).
 */
package touchy.pad.aspects;
